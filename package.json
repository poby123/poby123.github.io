{
  "name": "poby-blog",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "start": "npx live-server",
    "git-add" : "git add *",
    "git-commit" : "git commit -m",
    "deploy" : "git add * && git commit -m "
  },
  "dependencies": {
    "ejs": "3.1.6"
  },
  "devDependencies": {
    "live-server": "1.2.1"
  }
}